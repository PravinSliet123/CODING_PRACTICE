#include<bits/stdc++.h>
using namespace std;
int main()
{
    string a = "1011", b = "11";
    int aPtr = a.length()-1;
    int bPtr = b.length()-1;
    int i = 0;
    int j = max(aPtr, bPtr);
    bool carry = false;
    string temp = "";
    while(i<min(aPtr, bPtr))
    {
        
    }
    cout<<temp;
}